name: evening report (reusable)
on:
  workflow_call:
    inputs:
      force:
        description: "跳过 21:00 时刻门（手动触发/测试用）"
        required: false
        default: false
        type: boolean
    secrets:
      GLM_API_KEY:
        required: true

jobs:
  report:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout data vault (caller repo)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # quiet门/滑落/完成数/改动清单/stats 全部依赖 git 历史，浅克隆会静默清零

      - name: Checkout engine
        uses: actions/checkout@v4
        with:
          repository: ${{ github.repository_owner }}/planning-engine
          ref: main
          path: engine

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Run engine unit tests
        run: python -m unittest discover -s engine/tests

      - name: Generate evening report
        env:
          GLM_API_KEY: ${{ secrets.GLM_API_KEY }}
        run: |
          git config user.name "planning-bot"
          git config user.email "planning-bot@users.noreply.github.com"
          FORCE=""
          if [ "${{ inputs.force }}" = "true" ]; then FORCE="--force"; fi
          python engine/report.py --vault . --out reports/tomorrow.md --usage reports/usage.json $FORCE
          python engine/ics.py --vault . --out reports/deadlines.ics
          python engine/state.py --vault .

      - name: Commit report
        run: |
          git add reports/ 仪表盘.md
          if ! git diff --cached --quiet; then
            git commit -m "evening report $(TZ=America/Los_Angeles date +%F)"
            git pull --rebase --quiet || true
            git push
          else
            echo "report unchanged, nothing to commit"
          fi

      - name: Push notification (success)
        if: success()
        env:
          VAPID_PRIVATE_KEY: ${{ secrets.VAPID_PRIVATE_KEY }}
        run: |
          pip install -q pywebpush 2>/dev/null || true
          python engine/notify.py --vault . --kind evening || true

      - name: Push notification (failure)
        if: failure()
        env:
          VAPID_PRIVATE_KEY: ${{ secrets.VAPID_PRIVATE_KEY }}
        run: |
          pip install -q pywebpush 2>/dev/null || true
          python engine/notify.py --vault . --kind failure --msg "晚间报告任务失败，打开 GitHub Actions 查看日志（或让 ZCode 修）" || true
